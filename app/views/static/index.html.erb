<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>

<div class="unit two-of-three">
<h2>Next Photo Walk</h2>
<p class="date">Date: <%= Walk.last.start_date.strftime('%d-%m-%Y') if Walk.last.present? %></p>
<div class="google-maps">
  <div id="map"></div>
<script type="text/javascript">
handler = Gmaps.build('Google');
handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
  markers = handler.addMarkers(<%=raw @hash.to_json %>);
  handler.bounds.extendWith(markers);
  handler.fitMapToBounds();
  handler.getMap().setZoom(16)
});
</script>
</div>
  <p class="book"><a href="#">Book a spot</a></p>

  <div class="recent-uploads">
    <h2>Recent Uploads</h2>
      <p>Glasgow Photo Walk values its community and encourages participants of the photo walks to upload and share their photos. <%= link_to 'Sign up', new_photographer_registration_path %> and <%= link_to 'begin sharing', new_photo_path %>.</p>
        <% if @photo.present? %>
          <% @photo.sort_by{|t| - t.created_at.to_i}.first(5).each do |p| %>
          <%=  link_to p.image_url do %>
            <%= image_tag(p.image_url(:thumb).to_s) %>
          <% end %>
          <% end %>
        <% end %>

  <p class="button"><%= link_to 'Upload your photo', new_photo_path %></p>
  </div>
</div>

<div class="unit one-of-three sidebar">
<div class="about">
  <h3>What is this?</h3>
    <p>Glasgow Photo Walk is a structured yet informal walk around the city of Glasgow, requiring just two essentials - a camera and tolerance of rain! The photo walk isn’t restricted to dSLRs, and any level of photographer using any tool – even an iPhone – is encouraged to join us. What will you photograph? Whatever you want! Landscapes, street photography, or even photos of people taking photos. Anything goes.</p>
</div>

<% if Walk.last.present? %>
  <h3>Deets</h3>
  <ul>
	 <li>Date:		<%= Walk.last.start_date.strftime('%d-%m-%Y') %></li>
	 <li>Location: 	<%= Walk.last.address %></li>
	 <li>Time: 		<%= Walk.last.start_time.strftime("%k:%M %p") %></li>
	 <li>Length: 	<%= Walk.last.duration %> hour(s)</li>
	 <li>Guide: 		<a href="http://twitter.com/iamashley">Ashley Baxter</a></li>
	 <li>Price: 		<%= number_to_currency(Walk.last.price, :unit => "£") %></li>
    </ul>
<% end %>

  <div class="bio">
    <h3>Tour Guide Bio</h3>
      <%= image_tag("ashley.jpg", class: "avatar",  size: "115x115") %>
        <p class="profile">Ashley Baxter, otherwise known as <a href="http://girlwithacamera.co.uk">Girl With A Camera</a>, loves both photography and Glasgow, thinking the city is a versatile and interesting setting to shoot. Ashley has photographed campaigns for clients ranging from Glasgow City Marketing Bureau, Young Scot and Citizens Theatre, as well as doing wedding photography in the summer months.</p>
  </div>
</div>
